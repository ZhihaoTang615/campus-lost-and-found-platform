# Database Design

The system will use a MySQL database to store information about lost and found items.

Planned tables include:
- Users (future feature)
- Items (lost or found items)
- Claims (item ownership claims)

The database design will be further developed in later iterations.